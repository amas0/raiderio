from setuptools import setup

setup(name='raiderio',
      version='1.0',
      author='amas0',
      description='Python client library for raider.io\'s public API',
      install_requires=[
            'requests'
      ])
